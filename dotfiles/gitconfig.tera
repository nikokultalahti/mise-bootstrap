# .gitconfig (managed by mise via gitconfig.tera)

[user]
    name = Niko Kultalahti
    email = {% if mise_env is defined and "work" in mise_env %}{{ vars.email_work }}{% else %}{{ vars.email_personal }}{% endif %}
    signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIHTI6zoCvmPy+Rh0NnkOziJwDGVFRS0aBJyxUcZNqzNi
[color]
    ui = auto
[core]
{% if mise_env is defined and "work" in mise_env %}
    editor = nano
{% else %}
    editor = "flatpak run re.sonny.Commit"
{% endif %}
    excludesfile = ~/.gitignore
[gpg]
    format = ssh
[commit]
    gpgsign = true
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
{% if mise_env is defined and "personal" in mise_env %}
[re.sonny.Commit]
    title-length-hint = 50
    body-length-wrap = 72
{% endif %}
{% if mise_env is defined and "work" in mise_env %}
[credential "https://source.developers.google.com"]
    helper = gcloud.sh
[url "git@bitbucket.org:"]
    insteadOf = https://bitbucket.org/
{% endif %}
